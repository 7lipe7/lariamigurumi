// Exemplo de configuração — copie para js/config.js e preencha com os valores reais.
// (js/config.js está no .gitignore e nunca deve ser commitado)
// Obs.: a chave "anon" do Supabase é pública por design — a proteção real
// dos dados é feita pelas policies de RLS no banco.
window.ENV = {
    SUPABASE_URL: "https://SEU-PROJETO.supabase.co",
    SUPABASE_ANON_KEY: "SUA_CHAVE_ANON_AQUI",
};
